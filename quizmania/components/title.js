import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Title = () => {
    return (
        <View>
         <Text style={{textAlign:'center'}}>This is Quizzler</Text>
        </View>
    )
}

export default Title

const styles = StyleSheet.create({});
